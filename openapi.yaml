# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: HealthCheck API
    description: The HealthCheck service definition.
    version: 0.0.1
paths:
    /healthz:
        get:
            tags:
                - HealthCheck
            description: Getting a healthcheck status
            operationId: HealthCheck_HealthCheck
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/healthcheck.v1.HealthCheckResponse'
components:
    schemas:
        healthcheck.v1.HealthCheckResponse:
            type: object
            properties:
                status:
                    type: string
            description: The response message containing the health check status
tags:
    - name: HealthCheck
